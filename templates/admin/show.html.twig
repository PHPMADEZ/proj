{% extends 'base.html.twig' %}

{% block title %}AdminController{% endblock %}

{% block body %}
    <h1>AdminController</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_controller.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admin_controller.name }}</td>
            </tr>
            <tr>
                <th>Geboortedatum</th>
                <td>{{ admin_controller.geboortedatum ? admin_controller.geboortedatum|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin_controller.email }}</td>
            </tr>
            <tr>
                <th>Telnummer</th>
                <td>{{ admin_controller.telnummer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_edit', {'id': admin_controller.id}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
